import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LatexService {

  constructor() { }

  public translate(exp: string): string{
    // pending
    return exp;
  }
}
